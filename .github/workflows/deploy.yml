name: Deploy unified norelle.cc site

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    uses: vibeforge2014/portal/.github/workflows/deploy-pages.yml@main
    secrets:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
